# Ring-Reader 3.0.1 发布说明

> 伪装计算器 · 环间阅读器
> 构建产物：`zeus build` → `dist/`（全设备 .zab）

## 本次版本重点

### 1. 修复书架删除按钮点不到（关键 Bug）
此前在 Amazfit Balance 上，书架卡片右上角的「×」删除按钮点击**完全无响应**（连书都不打开），
说明该角落区域没有任何控件收到点击事件。

根因分析（Zepp OS 控件命中机制）：
- 透明的极小控件（`alpha:0` 的 FILL_RECT）在部分固件上不参与命中检测；
- 叠在可点控件**上层**的 `TEXT` 控件会挡住下方点击（TEXT 本身不是有效点击目标）；
- 角落极小按钮在圆形屏安全区边缘也容易失效。

修复方案（双保险，确保可靠）：
- **长按书卡删除（主方案）**：复用「已被验证可点」的整卡打开层，短按=打开，长按 550ms=删除。
  这是最可靠的删除入口，不受角落按钮限制。
- **× 按钮（辅助方案）**：圆点承载点击，× 文字画在**圆点下层**透过半透明圆点显示，
  保证顶层只有可点的圆点，不会被文字层挡住。点不到 × 时长按书卡同样可删除。

删除逻辑本身（与 3.0.1 之前一致并强化）：
- **手机传入（下载）小说**：同时移除书架记录与真实数据文件（多路径兼容 `data://`、去 `data/` 前缀等）。
- **内置小说**：安装包资源不能物理删除，通过 `deleted_books` 永久名单使其不再出现在书架（重启也不回来）。

### 2. 关于页新增开源仓库二维码
- 关于页卡片新增二维码，扫码直达：`https://github.com/ring-amazfit/Ring-Reader`
- 资源：`qr_github.png`（已放根目录与 `assets/`，构建自动打包）。
- 关于页改为「点击空白处关闭」，去掉原返回按钮，腾出空间放二维码。
- 关于页图标复用 `icon.png`（深石墨 + 琥珀色一致），失败回退「环」字徽标。

### 3. 其他本次一并交付的优化
- 翻页卡顿：预读机制（翻页优先、空闲 350ms 预读、缓存复用、正文控件只更新文字）。
- **菜单/跳页面板池化**：阅读器菜单（字号/行距/亮度/主题/自动/底部 6 按钮）与跳页数字键盘改为控件池模式，首次打开创建所有控件，后续开关只改 alpha 可见性 + setProperty 更新动态内容，配合 zeppos-fx 淡入淡出动画。原先每次开关都 deleteWidget+createWidget 30+ 控件，现在开关零延迟复用，只在字号/主题等需要重建正文时才销毁重建。
- 预计阅读时间：改为「本次会话有效前进速度」估算，不再被熄屏/跳页污染。
- 阅读正文排版/字号/行距/分页/布局**完全不变**（`READ_*` 参数未动）。
- 菜单、书架卡片、弹窗：统一为更扁平的石墨层级，减少黑色叠层与持续动画。
- 版本号：`app.json` code 4 / name 3.0.1；关于页 `v3.0.1 · 伪装计算器`，描述改「环间阅读器」。

## 支持设备
Balance / GTR4 / T-Rex 3 / Cheetah Pro / Active 2（圆屏安全区适配）。

## 构建
在 Windows（已装 Zepp OS Tools / Zeus）上：
```bat
cd /d D:\ring\ring-reader
zeus build
```
产物位于 `dist/`，通过 Zepp App 或 Amazfit 伴侣安装到手表。
